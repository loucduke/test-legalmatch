<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.5//EN"
    "http://struts.apache.org/dtds/struts-2.5.dtd">

<struts>
    <constant name="struts.action.extension" value="xhtml,,xml,json,action"/>
    
    <constant name="struts.mapper.class" value="org.apache.struts2.dispatcher.mapper.PrefixBasedActionMapper" />
    <constant name="struts.mapper.prefixMapping" value="/rest:rest, :struts"/>
    <constant name="struts.actionProxyFactory" value="org.apache.struts2.factory.PrefixBasedActionProxyFactory"/>
    
    <constant name="struts.convention.action.suffix" value="Controller"/>
    <constant name="struts.convention.action.mapAllMatches" value="true"/>
    <constant name="struts.convention.default.parent.package" value="rest-default"/>
    <constant name="struts.convention.package.locators" value="services"/>    
    <constant name="struts.rest.namespace" value="/rest" />
    
    <package name="default" namespace="/" extends="struts-default">
        <action name=""> 
          <result type="redirectAction">login</result> 
        </action>
    </package>

    <package name="rest" namespace="/rest" extends="rest-default">
        <action name=""> 
          <result type="redirectAction">login</result> 
        </action>
    </package>
        
</struts> 